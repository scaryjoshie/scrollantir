# macOS
.DS_Store

# Python (future: mac-forwarder, server)
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Admin CLI local config (contains service_role DSN)
scripts/.env.admin

# Local secrets inbox (minted tokens, DSNs — never commit)
scripts/.secrets/

# Node (future: dashboard)
node_modules/

# IDE
.vscode/
.idea/
*.iml

# Build artifacts
build/
dist/
*.log

# Runtime test output from the stub server
android-testing/received/
android-testing/.venv/

# Vendored reference code — clone locally, don't commit
vendor/

# Secrets / local config — never commit
secrets/
*.local
local.properties
